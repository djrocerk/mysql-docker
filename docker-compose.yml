version: '3.8'

services:
  mysql:
    container_name: mysql-container
    image: mysql:latest
    restart: always
    volumes:
      - ./mysql_data:/var/lib/mysql
    ports:
      - 3303:3306
    environment:
      - MYSQL_ROOT_PASSWORD:rcg2022